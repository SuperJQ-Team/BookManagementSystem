<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etoak.java.mapper.UsersMapper">
    <!--
    id = "blockUser" 将update标签的sql语句
    -->
    <update id="blockUser">
        update users set is_block = 1 where id = #{id} and is_block = 0
    </update>
    <update id="changeScore">
        update users set score = score + #{score} where stu_no = #{no};
    </update>
</mapper>