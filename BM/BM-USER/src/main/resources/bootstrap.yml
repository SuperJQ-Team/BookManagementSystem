#server:
  #port: 8083
  # 添加数据源
spring:
  #datasource:
   # url: jdbc:mysql://localhost:3306/bm-user
    #驱动配置
    #driver-class-name: com.mysql.cj.jdbc.Driver
    #username: root
    #password: Zzz116540503
  #type:com.alibaba.druid.pool.DruidDataSource
  application:
    name: BM-USER-SERVICE
  #配置文件后缀 BM-USER-SERVICE-dev
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        #是否开启配置中心 默认true
        enabled: true
        #文件的扩展名
        file-extension: yml
        group: bm